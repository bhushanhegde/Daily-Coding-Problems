#this question is based on the binary tree ,serialize and deserialize .
# so did not study that yet
#better i will try this question in future


#given the root to a binary tree, implement serialize(root), which serializes the tree into a string, and deserialise(s),
#deserializes s back to the tree

